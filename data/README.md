# Data

The raw M5 competition data is not stored in this repository.

Expected source files:

- `sales_train_evaluation.csv`
- `calendar.csv`
- `sell_prices.csv`

Raw and processed datasets should be stored outside GitHub, such as in Google Drive.
